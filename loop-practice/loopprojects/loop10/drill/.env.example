# Reference copy of the variable the task needs. DUMMY ONLY.
#   - For the LOCAL arm of the drill: copy this file to .env in this folder.
#   - For the REAL (cloud) usage: do NOT create .env - paste the value into the
#     platform's secret store (Routine env-var panel / GitHub Actions secret)
#     and let it arrive as an environment variable.
DUMMY_SECRET_TOKEN=DUMMY_SECRET_TOKEN_12345
