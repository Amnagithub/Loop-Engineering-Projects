# Progress

## Start (2026-09-01)

- Created `calculator.py` with three intentionally buggy functions: `add`, `subtract`, `multiply`.
- Created `test_calculator.py` with three tests that currently fail.
- Next step: run the test command inside a `/goal` loop and fix the bugs until all tests pass.

## Done ✅ (2026-09-01)

- Fixed all three bugs in `calculator.py`:
  - `add` now returns `a + b`
  - `subtract` now returns `a - b`
  - `multiply` now returns `a * b`
- The test command showed all **3/3 tests passing** (`test_add`, `test_subtract`, `test_multiply`).
- Project 2 is complete: make the tests pass, then stop.
